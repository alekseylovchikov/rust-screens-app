# Simple RUST tool for making screenshots

### How to build and run:

`cargo run` - this command will create a directory called "screens"

`cargon run -- {directory_name}` - this command will create a directory with the name you specify
